#include "push_swap.h"

int	main(int argv, char *argc[])
{
	int	i = 0;
	int j = 0;
	while(argc[++i])
	{
		if (ft_strchr(argc[i], ' '))
		{
			//treat it as a string and divide it
		}
		else
		{
			//apply atoi on the arguments
		}
	
		printf("%s\n", argc[i]);
	}
	return 0;
}